# Kaggriculture 強化学習のやり方

このフォルダの強化学習コードが「何を、どうやって」学習しているかの説明です。
3系統を試して、**どれも模倣学習の出発点を超えられませんでした**。その中身と、なぜ効かなかったかをまとめています。

- コード全体の地図：[CODE_GUIDE.md](CODE_GUIDE.md)（強化学習は §3）
- 作業の経緯：[WORKLOG_2026-09-17.md](WORKLOG_2026-09-17.md)（§4、§12〜14）
- 実行ログ・評価結果：`results/`（`runs/ppo1/log.jsonl` に反復ごとの成績とKL）

---

## 目次

1. [前提：何を学習しているか](#1-前提何を学習しているか)
2. [3系統に共通する土台](#2-3系統に共通する土台)
3. [系統①　GRPO型PPO（価値関数なし）](#3-系統grpo型ppo価値関数なし)
4. [系統②　PPO ＋ 価値関数 ＋ GAE](#4-系統ppo--価値関数--gae)
5. [系統③　選別自己模倣（expert iteration）](#5-系統選別自己模倣expert-iteration)
6. [結果と、共通する壁](#6-結果と共通する壁)
7. [次の一手](#7-次の一手)
8. [再現手順](#8-再現手順)

---

## 1. 前提：何を学習しているか

方策（policy）は模倣学習で作った **BCNet** そのものです。強化学習はゼロから学ぶのではなく、**模倣学習済みのモデルを出発点に微調整する**形を取っています。

### 1試合の構造

- 1試合 = 30日 × 24時間 = **720ステップ**
- 1ステップで決めること
  | 対象 | 中身 | 出力ヘッド |
  | --- | --- | --- |
  | ユニットの intent | 農家＋雇い人（最大15人）が「どのタイルで何をするか」。候補 1,707個（`N_CAND`） | 候補スコア |
  | 拾う個数 | PICKUP の個数 | 個数ヘッド |
  | 市場 | 品目ごとの売買数量 | 分類ヘッド 21個（`MARKET_HEADS`） |
- 合計すると **1試合で約7,000回の判断**

### 報酬

**試合終了時の1つだけ**です。

```
報酬 = clip((自分の所持金 − 相手の所持金) / 50,000, −1, +1)
```

（`VALUE_SCALE` = 50,000：[scripts/train.py:170](scripts/train.py:170)）

つまり **7,000回の判断に対して、手がかりは試合の最後に1つだけ**。これが3系統すべての結果を決めました。

---

## 2. 3系統に共通する土台

### 2-1. 試合の作り方（サンプリング）

学習には「少し違う手を打ってみて、結果を比べる」必要があります。BCエージェントに `sample=True` を渡し、最尤の手ではなく確率分布から**抽選**します（[kag/agents/bc.py:169](kag/agents/bc.py:169) の `_draw`）。

- 温度：ユニット `tau_u=0.5`、市場 `tau_m=1.0`
- **提出時は最尤（greedy）**。ここが学習時とずれている点は、系統③の失敗要因になりました

### 2-2. 判断の記録

`record=True` のとき、ネットワークへの入力・選んだ候補・そのときの対数確率をすべて保存します（[kag/agents/bc.py:322](kag/agents/bc.py:322)）。学習側が**同じ状況を再現して確率を計算し直せる**ようにするためです。記録には次も含まれます。

- `u_blocked`：他のユニットが先に取ったせいで選べなかった候補（ユニットは自信の高い順に決めるため）
- `u_train`：ガード（餌やり・世話・最終日）が強制した判断は**学習対象から外す**フラグ

### 2-3. 試合の回し方（まとめ実行）

1試合ずつ動かすと、1手の97%がGPU推論の待ち時間でした（110.7ms 対 特徴量1.2ms）。[kag/rl_batch.py](kag/rl_batch.py) は **K試合を横並びで1手ずつ進め、K試合分の入力を1回のGPU推論にまとめます**。

- 判断のコードは `BCPolicy.build_ctx()` / `decide()` をそのまま使う → **学習中の試合と提出物の挙動が完全に一致**
- **1試合 33〜170秒 → 約5〜6秒**
- 相手を `"self"` にすると自分の複製と対戦し、相手側の推論も同じまとめ呼び出しに同乗する（GPU的にはほぼ無料）
- 価値関数の出力 V(s) も同時に返す（GAE用）

確認方法：

```bash
python -m kag.rl_batch runs/v_lr03/best.pt 8 --check
```

1試合ずつ実行した場合と200手先まで行動が完全一致することを確認済みです。

### 2-4. 出発点から離れすぎない仕掛け（KLペナルティ）

学習前の模倣方策を凍結したコピー（`ref`）として持ち、更新後の方策との **KLダイバージェンスを損失に加えます**（係数 β=0.02）。合法手の分布上で厳密に計算します。強化学習で暴れて、模倣で得た形を壊すのを防ぐためです。

---

## 3. 系統①　GRPO型PPO（価値関数なし）

[scripts/rl_train.py](scripts/rl_train.py)　＋　[kag/rl_rollout.py](kag/rl_rollout.py)

### 考え方

**「同じ状況で6回打たせて、その中で良かった試合の手を強める」**

価値関数を使わず、同じ条件の試合同士を比べることを基準線にします。

### 手順

1. 1反復 = G グループ × K 試合（既定 4×6 = 24試合）
2. **1グループは (seed, 相手, 座席) を固定**。K試合の違いは抽選の結果だけ
3. 試合の評価値：`(自分 − 相手) / 10,000 ＋ 勝ち1.0 / 引分0.5 / 負け0`
4. **アドバンテージ = グループ内で標準化したその値**（±3にクリップ：[scripts/rl_train.py:61](scripts/rl_train.py:61)）
5. その1つの値を、**その試合の全判断に同じだけ配る**
6. 更新は PPO（クリップ0.2、2エポック、ミニバッチ64ステップ、lr 2e-5）＋ KLペナルティ

### 実装のメモ

- ロールアウトは別プロセス（8〜10本）で **numpy 実行**。torch を読み込まないのは、numpyのMKLとtorchのOpenMPが同居すると落ちるためです
- そのため反復ごとに重みを `cur.npz` に書き出してワーカーに渡します
- ユニット・市場に加えて「拾う個数ヘッド」も更新します（重み0.3）

### 結果

**40反復・960試合で 0.506 → 0.405〜0.440（悪化）**。1更新あたり24試合では、7,000判断への配分を支える情報がまったく足りませんでした。

---

## 4. 系統②　PPO ＋ 価値関数 ＋ GAE

[scripts/rl_ppo.py](scripts/rl_ppo.py)　＋　[kag/rl_batch.py](kag/rl_batch.py)

①の弱点（手がかりの配分・速度）を両方直した本命版です。

### 価値関数（critic）

モデルに小さな枝 `v_mlp` を足し、盤面と全体情報から「今どれくらい有利か」を tanh で −1〜+1 で出します。

- まず**模倣学習の時点で、お手本の試合結果を教師にして**学習（`train.py --value-weight`）
- 強化学習中も価値損失（係数0.5）で更新し続ける
- 価値ヘッドのないチェックポイントを渡すと停止します

### GAE による配分

報酬は最後の1回だけなので、各手の評価は「**その手の後で形勢の見積もりがどれだけ上がったか**」になります（[scripts/rl_ppo.py:73](scripts/rl_ppo.py:73)、γ=1.0・λ=0.95）。
「試合に勝ったから全部良い手」ではなく「この手で良くなった」で評価できるのがポイントです。

### 1反復の流れ

1. **48試合を同時に実行**。半分は自分の複製との自己対戦、半分は上位8チームのテープ相手（`LEAGUE`）
2. 各試合に GAE でアドバンテージを計算し、反復全体で正規化（±5にクリップ）
3. 更新前の重みで対数確率を計算し直す（`fill_old`）
4. PPO更新を2エポック、64ステップずつのミニバッチで
   ```
   損失 = ユニット方策損失 ＋ 市場方策損失 ＋ 0.5 × 価値損失 ＋ 0.02 × KL
   ```
   （[scripts/rl_ppo.py:277](scripts/rl_ppo.py:277)、lr 2e-5、勾配ノルム1.0でクリップ）
5. 5反復ごとに固定パネル（4チーム × 8試合）で評価し、チェックポイントを保存

### 結果

**121反復・5,808試合・約11時間で、評価 0.677 → 0.693（横ばい）**。振れ幅 0.594〜0.781 の中に埋もれています。
一方で **KL は 0.009 → 0.236 まで広がりました**。方策は確かに動いた、でも良くはならなかった、ということです。

> **実装の差分**：①には「拾う個数ヘッド」の方策損失（`pg_p`）がありますが、②の損失には入っていません（計算はするが未使用）。PICKUPの個数は判断全体のごく一部なので影響は小さいはずですが、違いとして残っています。

---

## 5. 系統③　選別自己模倣（expert iteration）

[scripts/self_imitate.py](scripts/self_imitate.py) → [scripts/si_merge.py](scripts/si_merge.py) → [scripts/train.py](scripts/train.py)

### 考え方

勾配ではなく**選別**を学習信号にします。**「たくさん打たせて、勝った試合だけをお手本として普通の模倣学習にかける」**。
試合が少ないときに壊れにくい、というのが狙いです。

### 手順

1. **生成**：`sample=True` で試合を打ち、②と同じまとめ実行で回す。各試合を**模倣学習のデータ形式（npz）でそのままディスクに書き出す**ので、試合をメモリに溜めません（初版は192試合で10GB使ってスワップしました）
2. **選別**：所持金差の上位25%を残す
3. **微調整**：選んだ試合を教師にして、出発点から `train.py --init` で3エポック（lr 2e-5）

### 選別の落とし穴

単純な上位取りだと「自分がうまく打った試合」ではなく「**相手が崩れた試合**」ばかり選ばれます。実際、700試合中174試合がテープ相手で、自己対戦は341試合中1試合しか残りませんでした。
対策として、**相手ごとに上位を取る層別選別**（`--stratify`）を追加しています。

### 結果（seed 840000〜、各128試合）

| モデル | 選別 | 更新量 | スコア |
| --- | --- | --- | --- |
| v_lr03（出発点） | — | — | **0.422** |
| si1_gentle | 層別 25% | 1エポック 5e-6 | 0.133 |
| si1_self | 自己対戦のみ 25% | 3エポック 2e-5 | 0.078 |
| si1_strat | 層別 25% | 3エポック 2e-5 | 0.070 |
| si1_rand | ランダム 25% | 3エポック 2e-5 | 0.031 |

選別ありはランダムの2倍なので**信号は存在します**。しかし「出発点から動かすこと自体の損失」のほうが大きい。
生成時は抽選（τ=0.5）、提出時は最尤なので、抽選の結果を真似ると確率分布がなだらかになり切れ味が落ちます。

---

## 6. 結果と、共通する壁

| | ① GRPO型 | ② PPO＋GAE | ③ 選別自己模倣 |
| --- | --- | --- | --- |
| 手がかりの配分 | グループ内の相対順位 | 価値関数＋GAE | 選別（試合単位） |
| 出発点 | `runs/bc_spa/best.pt` | `runs/v_lr03/best.pt` | `runs/v_lr03/best.pt` |
| 試合数 | 960 | 5,808 | 約700 |
| 計算時間 | — | 約11時間 | 生成2並列で数時間 |
| 結果 | 0.506 → 0.41〜0.44 | 0.677 → 0.693 | 0.422 → 0.03〜0.13 |
| 判定 | 悪化 | 横ばい | 大幅悪化 |

共通する壁：

- **1試合 約7,000回の判断に対して、手がかりは試合結果1つ**
- 参考にした上位解法は**150万〜1,250万試合**。手元の速度（2並列で1試合3.3秒）では150万試合に**57日**かかります
- フォーラムの報告でも、3億環境ステップ（約42万試合）回した参加者が最終資金8万で飽和（手元の模倣学習は9.2万、公開ルールベース v47 は9.5万）。**動いた強化学習でも、良いルールベースに届いていません**

評価そのものの注意点：

- 32〜64試合の比較では接戦を見分けられません（5ポイント差を示すには約385試合、2ポイントなら約2,400試合）
- 別の seed の組同士の数字は比べないでください。同じモデルが seed の組によって勝率 35.9%〜65.6% まで振れました

---

## 7. 次の一手

**強化学習には日単位の計画（30日ぶん＝約30回の判断）だけを決めさせ、実行は決定論的に行う。**

- 手がかりの密度が約240倍になります（7,000判断 → 30判断）
- 複数の参加者が独立にこの形に到達しています
- **未実装**

---

## 8. 再現手順

必要なもの（このフォルダには入っていません）：

- 出発点のチェックポイント：`runs/v_lr03/best.pt`（②③、価値ヘッド付き）、`runs/bc_spa/best.pt`（①）
- 対戦相手のテープ：`data/d0915`・`data/d0916`（②③）、`data/d0910`・`data/d0913`（①）
- 環境：Python 3.11、PyTorch（CUDA版）、`pip install kaggle-environments==1.32.7 --no-deps`

```bash
# まとめ実行の速度と、1試合ずつ実行した場合との行動一致の確認
python -m kag.rl_batch runs/v_lr03/best.pt 8 --check

# ① GRPO型PPO
python scripts/rl_train.py --init runs/bc_spa/best.pt --out runs/rl_spa --iters 40 --groups 4 --k 6

# ② PPO ＋ 価値関数 ＋ GAE
python scripts/rl_ppo.py --init runs/v_lr03/best.pt --out runs/ppo1 --iters 50 --games 48

# ③ 選別自己模倣：生成 → 統合・選別 → 微調整
python scripts/self_imitate.py --init runs/v_lr03/best.pt --out data/si_round1/sh0 \
    --games 400 --batch 24 --no-select --seed-start 2000000 --seed 3
python scripts/si_merge.py data/si_r1_strat --shards data/si_round1/sh0 data/si_round1/sh1 \
    --stratify --keep 0.25
python scripts/train.py data/si_r1_strat runs/si1_strat --d 256 --layers 6 --heads 4 \
    --bs 256 --epochs 3 --lr 2e-5 --init runs/v_lr03/best.pt
```

注意点（詳細は [README.md](README.md)）：

- torch と numpy（MKL）の行列計算を1プロセスで混ぜない（libiomp5md の重複で落ちる）
- `self_imitate.py` は1プロセスで3〜5GB使う。メモリ16GBのPCでは2並列まで
- 評価コマンドは JSON を含むので Git Bash から実行する（PowerShell の `Start-Process` では引用符が落ちる）
